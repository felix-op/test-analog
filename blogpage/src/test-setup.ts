import '@angular/compiler';
import '@analogjs/vitest-angular/setup-snapshots';
import '@analogjs/vitest-angular/setup-serializers';
import { setupTestBed } from '@analogjs/vitest-angular/setup-testbed';

setupTestBed();

/* 
 * RESUMEN DEL ARCHIVO:
 * Archivo de configuración para las pruebas automáticas (testing). Se ejecuta antes de correr las pruebas para preparar el entorno.
 * Comparación con Expo: En Expo, sería el archivo `jest.setup.js` que configuras para simular entornos nativos antes de probar si tus componentes fallan.
 */
