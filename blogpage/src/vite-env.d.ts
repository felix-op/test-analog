/// <reference types="vite/client" />

/* 
 * RESUMEN DEL ARCHIVO:
 * Este archivo le da instrucciones a TypeScript sobre cómo entender archivos que no son código (como importar imágenes) o variables de entorno creadas por el empaquetador (Vite).
 * Comparación con Expo: En un proyecto Expo con TypeScript, esto es idéntico a un archivo `app.d.ts` o `env.d.ts` que sirve para que TypeScript no tire error en rojo si haces un `import logo from './logo.png'`.
 */
